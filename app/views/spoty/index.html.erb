<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <h2>Las recomendaciones que traemos para ti</h2>
      <% if @songs.size == 0%>
        <p>No hemos encontrado nada para ti :(</p>
        <p>Dale like a un artista e intenta nuevamente</p>
      <% end %>
      <div class="pull-right btn-playlist-hide"><button id="hide" class="btn btn-success">Ocultar Playlist</button></div>
    </div>
    <div id="songs" class="row" >
      <% @songs.each do |song| %>
          <div class="col-sm-12 col-md-4">
             <iframe src="https://embed.spotify.com/?uri=spotify:track:<%= song %>" frameborder="0" allowtransparency="true"></iframe>
          </div>
      <% end %>
    </div>
  </div>
</div>
